# Given a singly linked list, find if the list is a circular linked list or not.
# A circular linked list is one where the last node of the list points back to the first node of the list.
# Example: A -> B -> C -> A -> B -> C -> A -> ...
# An empty list is considered a circular linked list.



if __name__ == '__main__':
    # llist = LinkedList()
    # llist.head = Node(1)
    # second = Node(2)
    # third = Node(3)
    # fourth = Node(4)

    # llist.head.next = second
    # second.next = third
    # third.next = fourth

    # if (Circular(llist.head)):
    #     print('Yes')
    # else:
    #     print('No')

    # fourth.next = llist.head

    # if (Circular(llist.head)):
    #     print('Yes')
    # else:
    #     print('No')
