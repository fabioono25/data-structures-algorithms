# given a circular linked list, count the number of nodes in it.
# example: A -> B -> C -> A -> B -> C -> A -> ... the number of nodes is 3


# if __name__=='__main__': 
 
#     # Initialize lists as empty */
#     head = None
#     head = push(head, 12)
#     head = push(head, 56)
#     head = push(head, 2)
#     head = push(head, 11)
 
#     print( countNodes(head))